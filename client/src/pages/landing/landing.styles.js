import styled from 'styled-components';

export const LandingPageContainer = styled.div`
    max-width: 100%;
    width: 100%100%;
    overflow: hidden;
    .main {
        padding: 20px;
    }
    overflow: hidden;
    .error {
       display: none;
    } 
`