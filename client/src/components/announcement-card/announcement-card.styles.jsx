import styled from 'styled-components';

import Card from '@material-ui/core/Card';

export const AnnouncementCardContainer = styled(Card)`
    margin-top: 20px;

    .avatar {
        background-color: #c62828;
        color: white;
    }

`